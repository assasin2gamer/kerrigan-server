////////////////////////////////////////////////////////////////////////////////
// data_mode.h
////////////////////////////////////////////////////////////////////////////////
#ifndef DATA_MODE_H
#define DATA_MODE_H

/*
 * Enum for choosing data mode: DEV (simulated data) vs REAL (live data).
 */
enum class DataMode {
    DEV,
    REAL
};

#endif // DATA_MODE_H
