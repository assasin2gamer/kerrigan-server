////////////////////////////////////////////////////////////////////////////////
// include/data_mode.hpp
//////////////////////////////////////////////////////////////////////////////
#ifndef DATA_MODE_HPP
#define DATA_MODE_HPP

/*
 * Enum for choosing data mode: DEV (simulated data) vs REAL (live data).
 */
enum class DataMode {
    DEV,
    REAL
};

#endif // DATA_MODE_HPP
